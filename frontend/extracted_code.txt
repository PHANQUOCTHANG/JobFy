<USER_REQUEST>
/**
 * CănCơ – Job Platform  |  HomePageV2.tsx
 * Aesthetic: Editorial Prestige – Playfair Display + Manrope, terracotta + amber
 * Sections: Announcement · Navbar · Hero · Marquee · Categories · Jobs ·
 *           Companies · CV Builder · How It Works · Testimonials · Blog ·
 *           App Download · Job Alert · Employer CTA · Footer
 */
import { useState, useEffect, useRef } from "react";
import {
  Search, MapPin, ArrowRight, ChevronRight, Bookmark, BookmarkCheck,
  Menu, X, Bell, Star, Users, Building2, Clock, Briefcase, TrendingUp,
  Code2, Palette, BookOpen, Heart, Headphones, DollarSign, LineChart,
  CheckCircle, Shield, Award, Sparkles, Send, Mail, Facebook, Linkedin,
  Youtube, Instagram, Phone, Globe, ArrowUpRight, Quote, FileText,
  Smartphone, ChevronLeft, Upload, Zap, BarChart3, Eye, BookMarked,
  CalendarDays,
} from "lucide-react";

// ─── Palette & tokens (reference only – used via inline styles) ──────────────
// terracotta: #D44E2B  |  amber: #E8A83A  |  ink: #111018  |  cream: #F7F4EE
// muted-text: #9B8E7F  |  border: #E8E2D8  |  card-bg: #FFFFFF

// ─── Types ───────────────────────────────────────────────────────────────────
interface Job {
  id: number; title: string; company: string; location: string;
  salary: string; type: string; tags: string[]; logo: string;
  posted: string; hot?: boolean; remote?: boolean;
}
interface Testimonial {
  id: number; name: string; role: string; company: string;
  quote: string; avatar: string; avatarBg: string; days: number;
}
interface Article {
  id: number; tag: string; title: string; excerpt: string;
  readTime: string; date: string; emoji: string; tagColor: string;
}

// ─── Static data ──────────────────────────────────────────────────
<truncated 45215 bytes>
   <p className="text-[#E8A83A] font-black text-xl" style={{ fontFamily: "'Playfair Display', serif" }}>94</p>
                        <p className="text-[10px] text-[#5A5048]">/ 100</p>
                      </div>
                    </div>
                  </div>
                  {/* CV Body */}
                  <div className="p-5 space-y-4">
                    {/* Skills */}
                    <div>
                      <p className="text-[11px] font-black text-[#D44E2B] uppercase tracking-widest mb-2.5">Kỹ năng nổi bật</p>
                      <div className="flex flex-wrap gap-1.5">
                        {["React", "TypeScript", "Next.js", "Node.js", "AWS", "Figma"].map(s => (
                          <span key={s} className="text-[11px] bg-[#F7F4EE] text-[#4A4040] border border-[#E8E2D8] px-2.5 py-1 rounded-md font-semibold">{s}</span>
                        ))}
                      </div>
                    </div>
                    {/* Experience bar */}
                    <div>
                      <p className="text-[11px] font-black text-[#D44E2B] uppercase tracking-widest mb-2.5">Sức mạnh hồ sơ</p>
                      {[
                        { label: "Kinh nghiệm", pct: 90 },
                        { label: "Kỹ năng",     pct: 85 },
                        { label: "Trình bày",   pct: 95 },
                      ].map(({ label, pct }) => (
                        <div key={label} className="mb-2">
                          <div className="flex justify-between mb-1">
                            <span className="text-[11px] text-[#6B6059] font-semibold">{label}</span>
                            <span className="text-[11px] text-[#D44E2B] font-bold">{pct}%</span>
                          </div>
                          <div className="h-1.5 bg-[#F0EBE3] rounded-full">
                            <div 
<truncated 36183 bytes>

NOTE: The output was truncated because it was too long. Use a more targeted query or a smaller range to get the information you need.